const ENVIRONMENTS = {
    PRODUCTION: 'production',
    INTEGRATION: 'integration',
    DEVELOPMENT: 'development',
};

const REDIS_KEYS_SUFFIX = 'CONFIG';

module.exports = { REDIS_KEYS_SUFFIX, ENVIRONMENTS };
